%h1 Listing words

%table
  %thead
    %tr
      %th Name
      %th Checked count
      %th
      %th
      %th

  %tbody
    - @words.each do |word|
      %tr
        %td= word.name
        %td= word.checked_count
        %td= link_to 'Show', word
        %td= link_to 'Edit', edit_word_path(word)
        %td= link_to 'Destroy', word, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Word', new_word_path
